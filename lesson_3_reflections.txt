Q:
When would you want to use a remote repository rather than keeping all your work local?

A:
While working on project that requires collaboration with others.

Q:
Why might you want to always pull changes manually rather than having Git automatically stay up-to-date with your remote repository?

A:
When yau want to work only on outdated version of an app.

Q:
Describe the differences between forks, clones, and branches. When would you use one instead of another?

A:
Branches->
It is non-linear list of commits, A repository can have many braches.

Clones->
These are two more copies of repository, store on same or diff computers

Fork->
Feature provided by Github.
It allows one user to generate a copie of another user's public repository on its own account.
Forking let the orignal user know its repository has been forked.

Q:
What is the benefit of having a copy of the last known state of the remote stored locally?

A:
User will have the up-to-date version of a repository.

Q:
How would you collaborate without using Git or GitHub? What would be easier, and what would be harder?

A:
Sending and recieving file through emails, or some similar severice.
Easier->
Didn't have to learn github.
Harder->
Manaing files and requests would be very difficult.
